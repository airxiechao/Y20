import userTokenMutations from './userToken.mutations'
import userTokenActions from './userToken.actions'

const userTokenStore = {
  state: {
  },
  mutations: userTokenMutations,
  actions: userTokenActions,
}

export default userTokenStore